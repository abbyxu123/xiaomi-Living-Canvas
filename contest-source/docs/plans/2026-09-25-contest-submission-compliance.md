# Living Canvas 赛事提交合规整理实施计划

> 按测试先行和证据先行原则执行；每项只提交已验证的事实。

## 任务 1：建立基线并修复可复现性问题

- 运行现有 C、后端、构建元数据和辅助原型测试，记录真实失败。
- 用现有失败用例覆盖 Pillow 兼容问题，最小修改资产生成器后复测。
- 构建后端 wheel，先证明资源缺失，再配置 package data，安装 wheel 后验证目录和控制台可读。
- 修正 `contest2026_000_hello_app` 的过期包名。

## 任务 2：整理 Gemini-S1 技术证据

- 从现有本地证据中提炼目标构建、整包封装、FEL 和 SPI NAND 的脱敏摘要。
- 新增 `docs/GEMINI_S1_ADAPTATION.md`，明确已完成、适配中和下一步验证门禁。
- 不纳入固件、NAND 备份、第三方烧录工具、私人路径或原始串口噪声。
- 运行证据隐私检查。

## 任务 3：按赛题要求重写提交说明

- 在 README 增加 openvela + ai_agent、自定义 Skill、主动感知与执行、端云协同、记忆、手机交接和 LVGL 的对应表。
- 把辅助原型移到低权重说明，并明确它不是 openvela 板端运行证据。
- 更新仓库角色：官方仓是正式提交，个人 fork 是 PR 工作副本，个人项目仓是备份。
- 确保所有状态和新证据一致。

## 任务 4：检查 AI Coding 日志

- 运行官方采集器的配置检查和会话列表。
- 只在存在真实、相关、无敏感内容的可归集会话时预览、确认并验证。
- 没有合格会话时保持 `logs/` 说明文件，不生成替代日志。

## 任务 5：全量验证与提交

- 重新运行全部测试和隐私扫描。
- 检查 Git diff、绝对私人路径、凭据特征、文档一致性。
- 使用带 DCO sign-off 的提交保留清晰历史。

## 任务 6：更新并合入现有 PR

- 将已验证提交推送到 `abbyxu123/contest2026_482_xingguangyinli` 的 `codex/gemini-s1-choice-ui`。
- 更新 PR #1 标题和正文，等待检查结果。
- 检查无阻塞项后合入 `open-vela/contest2026_482_xingguangyinli:dev-ai-contest-2026`。
- 最后核对官方分支提交，并在个人备份仓创建新的不可覆盖备份节点。
