# ESP32-S3 保底版烧录记录

时间：2026-09-20（Asia/Shanghai）

- 目标芯片：ESP32-S3 rev 0.2
- 目标 MAC：烧录前由操作者通过本机环境变量 `EXPECTED_MAC` 提供，不写入公开仓库
- 屏幕：CO5300，368×448 原生竖屏
- 触摸：CST820，初始化成功
- PSRAM：329,728 字节全屏帧缓冲分配成功
- SPIFFS：挂载成功，已用 6,318,925 / 9,028,721 字节
- 待机动画：`circle.mp4` 转换为 18 帧，450 ms/帧，帧 0–17 已连续循环并回到帧 0
- 菜单底图：使用正确的 `sitting.png`，外卖、盲盒、在家做饭三个热区已定位
- 交互骨架：单击高亮、二次确认、返回按钮、三条预设演示流程和结果自动返回待机已通过主机测试
- 写入校验：bootloader、分区表、boot_app0、应用和 SPIFFS 均通过 esptool Hash 校验

烧录脚本在读取并匹配上述 MAC 后才允许写入；其他串口不会被列为候选目标。启动日志已持续观察一分钟以上，未出现重启或资源读取错误。由于用户休息期间无人操作屏幕，触摸热区的实机点击校准将在下次协作时完成。
